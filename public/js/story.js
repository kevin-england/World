//get character choices on the page and on click ask the server for the story
//clear out holders
//create new elements
//append to holders

for (var i = 0; i < ./story.json.choices.length; i++) {
  if (e.target == 'BUTTON') {
    var choices = document.getElementById('button-holder');
    choices.addEventListener('click', function() {
      new xhrHTTPRequest = ''
      choices.getAttribute('data-choices')
    var newChoices = getAttribute('data-choices')
    newChoices = document.getElementById('button-holder');
    newChoices.style.display = 'block';
    }, false);
  };
}